# # 80: ACME + 跳转（浏览器访问也会先到 443，但证书续期需要 80）
# server {
#     listen 80;
#     server_name msh.zhaohuijian.com; # 你的域名

#     # 1. Certbot 验证专用路径
#     location ^~ /.well-known/acme-challenge/ {
#         access_log off;
#         root /var/www/certbot;
#         try_files $uri =404; # 如果路径不存在，返回 404
#     }

#     # 2. 强制跳转 HTTPS (一旦证书生成后取消注释)
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# server {
#     listen 443 ssl;
#     http2 on;
#     server_name msh.zhaohuijian.com;

#     ssl_certificate     /etc/letsencrypt/live/msh.zhaohuijian.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/msh.zhaohuijian.com/privkey.pem;
#     include /etc/nginx/ssl-params/options-ssl-nginx.conf;
#     ssl_dhparam /etc/nginx/ssl-params/ssl-dhparams.pem;

#     access_log off; # 关闭访问日志
#     error_log warn; # 只记录警告和错误

#     location / {
#         proxy_pass http://mingshihui-teacher:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # 避免 502: upstream sent too big header（通常是 Set-Cookie 等响应头过大）
#         # Next.js/认证库在某些会话下可能返回非常大的响应头（多条/超长 Set-Cookie）
#         # 这里提高到更稳的兜底值（只作用于该站点），避免偶发超限
#         proxy_buffer_size 256k;
#         proxy_buffers 16 256k;
#         proxy_busy_buffers_size 512k;

#         proxy_buffering off;
#         proxy_request_buffering off;

#         proxy_read_timeout 3600s;
#         proxy_send_timeout 3600s;
#     } 

#     # ACME 验证专用路径
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#         allow all;
#     }
# }